# เอกสารออกแบบระบบ — SNK-CD War Room

เอกสารนี้อธิบาย **ภาพรวมการออกแบบทั้งระบบ** ของ SNK-CD War Room (ห้องสถานการณ์งบประมาณ
สำนักงานพัฒนาชุมชนจังหวัดสกลนคร) เพื่อให้ผู้ที่มาดูแล/พัฒนาต่อเข้าใจว่าระบบทำงานอย่างไร
ตัดสินใจออกแบบแบบนี้เพราะอะไร และจะต่อยอดตรงไหนได้บ้าง

> เอกสารประกอบอื่น: [README.md](README.md) (วิธีใช้งาน + สถานะเฟส), [CLAUDE.md](CLAUDE.md)
> (บริบทกรมฯ + persona + feature list), [google-apps-script/README-การติดตั้ง.md](google-apps-script/README-การติดตั้ง.md)
> (คู่มือติดตั้งเฟส 2)

---

## 1. ระบบนี้คืออะไร

ระบบควบคุมและมอนิเตอร์โครงการ/งบประมาณ ที่สำนักงานพัฒนาชุมชนจังหวัดสกลนครได้รับจัดสรร
**จากทุกแหล่งงบประมาณ** ในปีงบประมาณ พ.ศ. 2569 (1 ต.ค. 2568 – 30 ก.ย. 2569)

- เป็นเว็บแอปหน้าเดียว (Single Page App) เปิดในเบราว์เซอร์ได้ทันที ไม่ต้องติดตั้ง ไม่ต้องมี server
- ประมวลผลทั้งหมด **ในเบราว์เซอร์ของผู้ใช้เอง** (client-side) — เฟส 1 อ่านจาก seed data ที่ฝังมากับแอป
- รองรับมือถือ/แท็บเล็ต/คอมพิวเตอร์ (responsive) และมีทั้งโหมดสว่าง/มืด

### ผู้ใช้ (persona)

เจ้าหน้าที่พัฒนาชุมชนทุกระดับ — ผู้บริหารระดับจังหวัด, ผู้บริหารระดับอำเภอ, เจ้าหน้าที่จังหวัด,
เจ้าหน้าที่อำเภอ — เป็นเครื่องมือ **ฝ่ายในราชการ (back-office)** ไม่ใช่ระบบที่ประชาชนเข้าใช้โดยตรง

---

## 2. หลักการออกแบบ (ทำไมถึงออกแบบแบบนี้)

หลักการเหล่านี้เป็น "เจตนา" ที่อยู่เบื้องหลังทุกการตัดสินใจ ผู้ดูแลต่อควรรักษาไว้:

1. **ไม่เพิ่มภาระงานให้ผู้กรอกข้อมูล** — ระบบอ่านจากไฟล์ Excel/Google Sheets ที่เจ้าหน้าที่การเงิน
   ทำอยู่แล้วทุกวัน ไม่มีแบบฟอร์มใหม่ให้ใครกรอกเพิ่ม
2. **เตือนล่วงหน้าเพื่อสนับสนุน ไม่ใช่จับผิด** — ภาษา/สถานะสีทั้งหมดสื่อว่า "ควรสนับสนุนเร่งด่วน"
   ไม่ใช่ "อำเภอนี้แย่" ไม่มีการจัดอันดับเชิงลงโทษ
3. **โปร่งใส ไม่พังเงียบ** — ข้อมูลผิดปกติ (#REF!, เบิกเกิน 100%, นับซ้ำ) จะถูก **แยกออกและแจ้งเตือน**
   ให้คนตรวจสอบ ไม่เคยแสดงเป็นเลข 0 เงียบๆ หรือปกปิด
4. **แสดงคุณภาพคู่ปริมาณ** — ไม่ตัดสินอำเภอด้วย %เบิกจ่ายอย่างเดียว แต่แสดงเหตุผล/บริบทประกอบ
   (เอกสารครบไหม, สัญญายืมเงินค้างไหม) เสมอ
5. **ค่าที่อนุมาน/จำลอง ต้องติดป้ายกำกับชัดเจนเสมอ** — ห้ามนำเสนอค่าที่ระบบเดา (เช่น กลุ่มงาน)
   หรือข้อมูลตัวอย่าง (mock) ว่าเป็นข้อเท็จจริง (ดูข้อ 9)
6. **แยกแหล่งข้อมูลออกจากหน้าจอ** — เปลี่ยนจาก seed data เป็น Google Sheets จริงได้โดยไม่ต้องแก้ UI

---

## 3. สถาปัตยกรรม 3 เฟส

ระบบออกแบบให้เติบโตเป็น 3 เฟส โดย **โครงหน้าจอไม่ต้องแก้เมื่อขึ้นเฟสใหม่**:

| เฟส | สถานะ | รายละเอียด |
|---|---|---|
| **เฟส 1 — Prototype** | ✅ ใช้งานได้ | อ่านจาก seed data ฝังในไฟล์ JS · ประมวลผลในเบราว์เซอร์ · ไม่มี backend |
| **เฟส 2 — ETL จริง** | 🟡 โค้ดพร้อม ยังไม่ deploy | Google Apps Script อ่าน Google Sheet จริง → ส่ง JSON contract เดียวกับ seed |
| **เฟส 3 — เชื่อมสด + แจ้งเตือน** | ⬜ วางแผนไว้ | ใส่ Apps Script URL ใน `config.js` → ดึงข้อมูลจริงอัตโนมัติ + แจ้งเตือนผ่าน LINE OA |

### กลไกที่ทำให้สลับเฟสได้โดยไม่แก้ UI: Data Provider Layer

`app/js/provider.js` มี 2 provider ที่คืนค่า **โครงสร้างข้อมูลเดียวกัน**:

```
SeedDataProvider     → อ่านจาก window.SEED_* (เฟส 1)
AppsScriptProvider   → fetch JSON จาก Apps Script Web App (เฟส 2/3)
```

`pick()` เลือก provider อัตโนมัติ: ถ้า `window.APP_CONFIG.appsScriptUrl` ถูกตั้งค่า → ใช้ Apps Script
ไม่งั้น → ใช้ seed data — **หน้าจอ (app.js) ไม่รู้และไม่สนใจว่าข้อมูลมาจากไหน**

### ชั้นการทำงาน (layers)

```
ข้อมูลต้นทาง (Excel / Google Sheets)
        │
        ▼
Data Provider (provider.js)  ── seed ⇄ Apps Script สลับได้
        │  { budget, quality, kokNongNa, projects, regulations, lastUpdated }
        ▼
ETL / Data Layer (etl.js)    ── pure logic ไม่แตะ DOM
        │  buildModel() → โมเดลรวมสำหรับ UI
        ▼
UI Layer (app.js + charts.js + export*.js)  ── hash router + renderer
        │
        ▼
หน้าจอ (9 เมนู) + ปุ่ม Export
```

---

## 4. โครงสร้างไฟล์

```
app/
  index.html              โครงหน้า (sidebar ซ้าย + topbar + main) + โหลด script ตามลำดับ
  css/style.css           ระบบดีไซน์ทั้งหมด (design tokens, สเกลตัวอักษร 8 ระดับ, light/dark)
  js/config.js            ★ จุดเดียวที่แก้ตอนขึ้นเฟส 3 (appsScriptUrl) + ค่า BPM mock
  js/etl.js               Data layer — กฎทอง, กรอง, ตรวจสอบ, คำนวณ (703 บรรทัด, ไม่แตะ DOM)
  js/provider.js          ชั้นแหล่งข้อมูล (seed ⇄ Apps Script)
  js/charts.js            กราฟ SVG ล้วน (เส้น/โดนัท/แท่งจัดอันดับ/แผนที่จังหวัด) ไม่พึ่งไลบรารี
  js/export.js            Export Wizard — รายอำเภอ × ประเภทงบ → .xlsx (ExcelJS)
  js/export-projects.js   Export Wizard — รายโครงการ → .xlsx (คู่ขนานกับ export.js)
  js/app.js               UI ทุกหน้าจอ (943 บรรทัด) — NAV, router, render ทุกหน้า
  data/*.js               seed data (budget/quality/kokNongNa/projects/regulations)
  data/sakon_nakhon_map.js ขอบเขต 18 อำเภอจริง (จาก OpenGISData-Thailand)
  vendor/exceljs.min.js   ไลบรารีสร้าง Excel (ฝังในเครื่อง ไม่พึ่ง CDN)

tests/tests.html          Unit tests 25 ข้อ (custom harness) ครอบคลุมกฎทอง + logic ใหม่
google-apps-script/       เฟส 2: Code.gs (ETL จริง) + README-การติดตั้ง.md
tools/                    เครื่องมือ: build_projects_seed.ps1 (แกะโครงการจาก Excel) + งาน docx/pptx
budget_summary.json ฯลฯ   ไฟล์ seed ต้นฉบับ (JSON)
```

**ลำดับโหลด script ใน index.html สำคัญ:** seed data → vendor → config → etl → provider →
export → charts → app (แต่ละไฟล์มี `?v=` cache-buster ปรับเลขทุกครั้งที่แก้)

---

## 5. Data Layer (etl.js) — หัวใจของระบบ

ไฟล์นี้เป็น **pure logic ไม่แตะ DOM เลย** UI เรียกผ่าน `window.ETL` ทั้งหมด ทดสอบแยกได้ง่าย

### กฎทอง 5 ข้อ (จากการวิเคราะห์ไฟล์ต้นทางจริง)

1. นับเป็น "ข้อมูล" เฉพาะแถวที่อยู่ในขอบเขตตารางที่ระบุ header ได้
2. คอลัมน์/เซลนอกขอบเขต (ที่ทดเลข, วันโอนเงิน) ห้ามปนเข้าคอลัมน์ตัวเงิน
3. แถวหมายเหตุ/เชิงอรรถใต้ตาราง ตัดออกจากข้อมูลรายอำเภอ
4. มี unit test ยืนยันว่า parser ไม่ดึง noise เข้ามา
5. **ห้ามมี source of truth สองจุดต่อโครงการเดียวกัน** — โคกหนองนา/จปฐ. เป็น Enrichment Layer
   เท่านั้น ห้ามบวกยอดเงินซ้ำ (dedupe ด้วย เลขที่โครงการ + อำเภอ + ไตรมาส)

### ฟังก์ชันหลัก (export จาก window.ETL)

| ฟังก์ชัน | หน้าที่ |
|---|---|
| `canon(s)` | normalize ชื่ออำเภอ: ตัดช่องว่าง + ยุบตัวอักษรซ้ำติดกัน (แก้ "กุุสุมาลย์" → "กุสุมาลย์") |
| `resolveUnit(name)` | จับชื่ออำเภอที่สะกดเพี้ยนเข้ากับ 18 อำเภอ master + หน่วย "จังหวัด" |
| `fiscalInfo(now, be)` | คำนวณจังหวะเวลาปีงบ (วันที่ผ่านไป/เหลือ, %ที่ควรเบิกแล้ว ณ วันนี้) |
| `paceStatus(actual, expected, threshold)` | คืนสถานะสี green/yellow/red/gray เทียบจังหวะเวลา (ไม่ใช่เกณฑ์ตายตัว) |
| `officialScore(pct, docsComplete, overdue)` | **สูตรคะแนนราชการเดิม ห้ามเปลี่ยน** (0–5) |
| `healthIndex(agg, quality, expected, weights)` | ดัชนีสุขภาพ 0–100 (น้ำหนักปรับได้: จังหวะ/ระเบียบ/ข้อมูล) |
| `processBudget(rows, fy)` | กรอง noise, ล็อกปีงบ 2569, ดัก #REF!, ดัก anomaly, กันงบจังหวัดออกจากระดับอำเภอ |
| `processQuality(rows)` | ประมวลแบบประเมินที่ 1 + ตรวจว่าคะแนนในไฟล์ตรงเกณฑ์ที่คำนวณได้ |
| `processProjects(entries)` | แกะรายโครงการ, dedupe ข้ามชีต, อนุมานกลุ่มงาน/ระดับ/ตัวชี้วัด/เอกสารแนบ |
| `processKokNongNa(rows)` | Enrichment layer (จำนวนแปลง/ค่าควบคุมงาน) — ห้าม sum เข้ายอดรวม |
| `processRegulations(rows)` | กรองรายการระเบียบที่ id/title ไม่ครบ |
| `inferWorkGroup(name)` | เดากลุ่มงานจาก keyword ในชื่อโครงการ (คืน `workGroupInferred` ด้วย) |
| `matchQuery(q, regs, projects)` | จับคู่คำสำคัญ (ใช้ในเมนู "ถาม AI" — ไม่ใช่ AI จริง) |
| `aggregate(rows)` | รวมยอด YTD ตาม หน่วย×หมวด / หน่วย / หมวด / จังหวัด |
| `buildModel(seed, config, now)` | **จุดประกอบทั้งหมด** → โมเดลเดียวที่ UI ใช้ render ทุกหน้า |

### ระบบตรวจข้อมูลผิดปกติ (issue types)

ทุก issue ที่ตรวจพบจะไปโผล่ในเมนู "แจ้งเตือนล่วงหน้า" หมวด "ข้อมูลต้องตรวจสอบ":

| type | ความหมาย |
|---|---|
| `REF_ERROR` | สูตร #REF! เสียหาย (อ้างชีตที่ถูกลบ) — ตัวเลขหมวดนี้ใช้ไม่ได้ ไม่นำมารวม |
| `ANOMALY` | เบิกจ่าย > จัดสรร (เกิน 100%) หรือคงเหลือติดลบ — น่าจะพิมพ์ผิด |
| `WRONG_FISCAL_YEAR` | พบข้อมูลปีงบอื่นปนมา (ระบบใช้เฉพาะ 2569) |
| `PROVINCE_ONLY_VIOLATION` | พบแถวรายอำเภอในงบจังหวัด/กลุ่มจังหวัด (ซึ่งมีเฉพาะระดับจังหวัด) |
| `DUP_PROJECT` | โครงการเลขที่เดียวกันปรากฏหลายชีต — นับจากชีตแรกเท่านั้น |
| `SCORE_MISMATCH` | คะแนนในไฟล์ไม่ตรงกับเกณฑ์ที่คำนวณได้ |
| `BAD_REGULATION` | รายการระเบียบที่ไม่มี id/title ครบถ้วน |

---

## 6. หน้าจอ / เมนู (UI Layer)

`app.js` ใช้ **hash-based router** (`location.hash` → `route()`) มี 9 เมนูอยู่ในแถบซ้าย (sidebar)
บนจอกว้าง และยุบเป็นแถบบนเลื่อนแนวนอนบนมือถือ (breakpoint 860px)

| # | เมนู | เนื้อหา |
|---|---|---|
| 🏠 | ภาพรวมจังหวัด | การ์ดสรุป (งบรวม, %เบิกจ่ายตัวใหญ่+ชิปสถานะ, สถานะ 18 อำเภอ, วันเหลือ) + **แผนที่จังหวัด** ระบายสีตามสถานะ + กราฟแท่งรายหมวดงบ + จุดที่ควรติดตาม |
| 📍 | รายอำเภอ | กราฟแท่งจัดอันดับ 5 อันดับสูงสุด + ตารางครบ 18 อำเภอ (กดเข้าดูรายละเอียดเจาะลึกได้) |
| 📋 | รายโครงการ | ตารางทุกโครงการจริง (92 โครงการ/กิจกรรม) กรอง/ค้นหาได้ + การ์ด BPM mock + ปุ่ม Export รายโครงการ · แต่ละแถวขยายดู KPI/เอกสารแนบ/ระเบียบที่เกี่ยวข้องได้ |
| 🗂️ | กลุ่มงาน | จัดกลุ่มโครงการตาม 4 กลุ่มงาน + กราฟเทียบ %เบิกจ่ายระหว่างกลุ่ม |
| ⭐ | มิติคุณภาพ | คะแนนราชการ 0–5 ทุกอำเภอ พร้อมเหตุผล + กราฟจัดอันดับ 5 อันดับ |
| 🔔 | แจ้งเตือนล่วงหน้า | ความเสี่ยงที่ควรสนับสนุน + "ข้อมูลต้องตรวจสอบ" (issue ทั้งหมด) |
| 📚 | ระเบียบ/แนวทาง/คู่มือ | ตารางระเบียบ แยกป้าย "อ้างอิงจริง" vs "ตัวอย่างประกอบ (จำลอง)" |
| 🤖 | ถาม AI | ค้นระเบียบ/โครงการด้วยคำถามภาษาพูด (ต้นแบบจับคู่ keyword — ไม่ใช่ AI จริง) |
| ⚙️ | ตั้งค่า | ปรับ threshold สถานะเหลือง + น้ำหนักดัชนีสุขภาพ (ผู้บริหารปรับเองได้) |

---

## 7. กราฟและแผนที่ (charts.js)

วาดด้วย **SVG ล้วนๆ ไม่พึ่งไลบรารีภายนอก** — สีตรงกับสถานะ/ชุดสีที่ผ่านการตรวจ contrast/CVD แล้ว
มี legend/label เสมอ (ไม่ใช้สีเดี่ยวๆ สื่อความหมาย):

| ฟังก์ชัน | ใช้ที่ไหน |
|---|---|
| `paceLineChart()` | กราฟเส้นแนวโน้มเบิกจ่ายสะสม เทียบเส้นเป้าวันนี้ |
| `donutChart()` + `legendHtml()` | สัดส่วนงบตามประเภท/กลุ่มงาน |
| `rankedBars()` | แท่งจัดอันดับแนวนอน (อำเภอ/กลุ่มงาน/คะแนนคุณภาพ) |
| `provinceMapSVG()` | **แผนที่จังหวัดสกลนคร** ระบายสี 18 อำเภอตามสถานะ + ชื่ออำเภอทุกอำเภอ (วางที่ centroid จริง) |

**ที่มาข้อมูลแผนที่:** ขอบเขตอำเภอจริงจาก [OpenGISData-Thailand](https://github.com/chingchai/OpenGISData-Thailand)
(ข้อมูลเปิดของหน่วยงานราชการไทย) กรองเฉพาะ 18 อำเภอสกลนคร เก็บใน `app/data/sakon_nakhon_map.js`
ถ้าไฟล์แผนที่โหลดไม่สำเร็จ ระบบ fallback กลับไปกริดตารางเดิมอัตโนมัติ

---

## 8. การส่งออก Excel (export)

Export Wizard 3 ขั้นตอน (ขอบเขต → เลือกคอลัมน์ → พรีวิว+ดาวน์โหลด) สร้างไฟล์ .xlsx **จริง**
ด้วย ExcelJS (ตรึงแถวหัว, ตัวกรองอัตโนมัติ, สีสถานะตรงกับหน้าจอ, ฟอนต์ TH Sarabun New):

- `export.js` → **รายอำเภอ × ประเภทงบ** (ปุ่มลอย FAB ทุกหน้า)
- `export-projects.js` → **รายโครงการ** (ปุ่มเฉพาะหน้ารายโครงการ)

---

## 9. โมเดลข้อมูลและสูตรสำคัญ (ห้ามเปลี่ยนโดยไม่ตั้งใจ)

- **ปีงบประมาณ 2569** = 1 ต.ค. 2568 – 30 ก.ย. 2569 (ระบบล็อกใช้เฉพาะปีนี้)
- **3 หมวดงบ:** งบบริหาร, งบยุทธศาสตร์, งบจังหวัด/กลุ่มจังหวัด — โดย **งบจังหวัด/กลุ่มจังหวัด
  มีเฉพาะระดับจังหวัด** ห้ามแสดง/คำนวณระดับอำเภอ
- **ไตรมาสเป็นวงเงินคนละก้อน:** ไตร 1-2, ไตร 3-4, ไตร 4 ไม่ใช่ช่วงเวลาต่อเนื่อง (งบบริหาร ไตร 4 แยกจาก ไตร 3-4)
- **สถานะสี = เทียบจังหวะเวลา** ไม่ใช่เกณฑ์ตายตัว: เขียว=ตามแผน/เร็วกว่า, เหลือง=ตามหลังเล็กน้อย,
  แดง=ควรสนับสนุนเร่งด่วน, เทา=รอข้อมูล, **ม่วง=ข้อมูลควรตรวจสอบ** (มีตัวเลขผิดปกติ ยังเชื่อไม่ได้)
- **คะแนนราชการ (officialScore):** ฐานจาก %เบิกสะสม (≥80%=1 … ≥100%+เอกสารครบ=5) หักเมื่อ
  สัญญายืมเงินค้างเกิน 30 วัน (1–4 สัญญา หัก 1 ระดับ, เกิน 4 หัก 2 ระดับ)
- **ดัชนีสุขภาพ (healthIndex):** ถ่วงน้ำหนัก จังหวะเบิกจ่าย 50 / ระเบียบ-เอกสาร 30 / ความสมบูรณ์ข้อมูล 20 (ปรับได้)

### หลักความโปร่งใส — ค่าที่ระบบเดา/จำลอง ต้องติดป้ายเสมอ

เพราะข้อมูลบางส่วนยังไม่มีจริง ระบบจึง **ติดป้ายกำกับชัดเจนทุกจุด** ไม่นำเสนอเป็นข้อเท็จจริง:

| สิ่งที่เดา/จำลอง | ป้ายกำกับ |
|---|---|
| กลุ่มงานที่ไม่มี keyword ตรง (fallback) | "(อนุมาน)" |
| กรอบตัวชี้วัดต่อโครงการ | "ตัวอย่างกรอบตัวชี้วัด (จำลอง)" |
| ระเบียบที่ไม่ใช่ของจริง | "ตัวอย่างประกอบ (จำลอง)" (แยกจาก "อ้างอิงจริง") |
| เอกสาร PDF อนุมัติโครงการ | "ตัวอย่าง — ยังไม่ได้แนบไฟล์จริง" (ไม่มีเอกสารปลอม) |
| การเชื่อมระบบ BPM กรมฯ | "ตัวอย่าง — ยังไม่ได้เชื่อมต่อจริง" (bpm.cdd.go.th มีจริง แต่ยังไม่มีสิทธิ์เชื่อม) |
| เมนู "ถาม AI" | "ต้นแบบ: จับคู่คำสำคัญอัตโนมัติ — ยังไม่เชื่อมต่อ AI จริง" |

---

## 10. การทดสอบ

`tests/tests.html` มี **unit test 25 ข้อ** (custom harness `test()`/`assert()`) ที่อ้างอิงตัวเลขจริง
จาก seed data เสมอ (ไม่ใช่ smoke test) ครอบคลุม:

- กฎทองทั้ง 5 ข้อ (กรอง noise, ไม่นับซ้ำ, #REF! ไม่พังเงียบ, ฯลฯ)
- normalize ชื่ออำเภอสะกดเพี้ยน, join คุณภาพครบ 18 อำเภอ
- กฎปีงบ 2569, กฎงบจังหวัดระดับจังหวัด
- dedupe ข้ามชีต (เลขที่ 52), รวมกิจกรรมย่อยในชีตเดียวกัน
- สูตรคะแนนราชการ, pace, การ flag คะแนนไม่ตรงเกณฑ์
- อนุมานกลุ่มงาน/ระดับ, matchQuery ของเมนูถาม AI

> **ต้องรัน `tests/tests.html` ให้เขียวครบก่อน deploy ทุกครั้ง** โดยเฉพาะเมื่อโครงสร้างไฟล์ต้นทางเปลี่ยน

---

## 11. แผนเฟสถัดไป

- **เฟส 2 (โค้ดพร้อม):** ทำตาม [google-apps-script/README-การติดตั้ง.md](google-apps-script/README-การติดตั้ง.md)
  — ย้ายชีตจริงขึ้น Google Sheets → deploy Apps Script → ใส่ URL ใน `config.js`
- **เฟส 3:** เชื่อมข้อมูลสด + แจ้งเตือนสรุปรายสัปดาห์ผ่าน LINE OA + เชื่อมระบบ BPM จริงของกรมฯ
- **ข้อควรพัฒนาที่ยังค้าง (จาก stress test):**
  1. เพิ่ม error handling ที่ `refresh()` ใน app.js — ปัจจุบันถ้า Apps Script ล่ม หน้าจอค้างเงียบ
  2. เพิ่มช่องให้เจ้าหน้าที่แก้ไข/ยืนยันกลุ่มงานที่ถูก "(อนุมาน)" เองใน UI
  3. วางแผนการส่งต่อผู้ดูแลระบบระยะยาว (ประเด็นเชิงองค์กร)
